# fail_report_1: 직무일지 자동분류 및 작성 시스템 개발 실패 및 점검 보고서

## 1. 프로젝트 개요 및 구현 목표
본 프로젝트는 업무 캡처 이미지(스크린샷) 또는 텍스트 메모를 입력받아, 사내 표준 직무 체계(`교육행정 업무.xlsx`)에 맞춰 **직무 분류, 상위 업무 목표 추천, 그리고 직무일지 자동 작성**을 수행하는 웹 기반 자동화 도구를 구현하는 것을 목표로 한다.

### 핵심 요구사항
1. **입력 편의성 (Chat UI)**
   - 대화형 인터페이스(Gemini 채팅창 형태) 지원.
   - 단축키(`Ctrl + V`)를 통한 클립보드 스크린샷 즉각 첨부 및 드래그 앤 드롭 지원.
   - 텍스트 입력과 이미지 첨부를 한 번에 묶어 전송(`Enter` / 전송 버튼).
2. **업무 목표 등록 및 직무 자동 분류**
   - 사내 표준 기준표(`직군 / 직종 / 직렬 / 직무`) 69개 항목과 대조하여 자동 매핑.
   - 업무 목표는 상세 내용을 과도하게 포함하지 않고, 방향성을 나타내는 **상위 목표 1문장**으로 요약.
   - 엑셀 시트에 포함되지 않는 비표준 업무는 자동으로 **'기타'**로 분류하고, 업무 목표 등록에서 제외.
3. **직무일지 작성**
   - **업무 내용**: 1~2문장 내외의 간결한 핵심 요약 (`1. `, `2. ` 번호 매기기 형식).
   - **특이사항**: 주요 결과 및 후속 조치 사항 (`1. `, `2. ` 번호 매기기 형식).
   - **기타 업무**: 시트 미포함 업무 발생 시 별도 '기타 업무' 영역에 기재.

---

## 2. 현재 발생 중인 실패 및 병목 지점 분석

### ① 로컬 환경 브라우저 직접 호출과 Gemini API 엔드포인트/모델 불일치
* **현상**:
  - `gemini-1.5-flash`, `gemini-2.5-flash` 호출 시 `404 Not Found` 또는 `model is no longer available to new users` 에러 반환.
  - 신규 발급된 API 키와 구글 엔드포인트 버전(`v1` vs `v1beta`) 간의 모델 식별자 불일치 발생.
* **원인**:
  - 클라이언트 사이드(`file:///` 브라우저 환경)에서 순수 `fetch()`로 구글 AI Studio 엔드포인트를 직접 찌를 때, 최신 무료 등급 계정에 할당된 모델 식별자가 실시간 변동되거나 레거시 모델 접근이 차단됨.

### ② 멀티모달 프롬프트 인식 실패 (무응답 / '기타' 기본값 처리)
* **현상**:
  - API 호출 에러가 발생하지 않더라도, 이미지만 전송되었을 때 모델이 문맥을 충분히 추출하지 못하고 모두 `기타` 및 `- 해당 없음 -`으로 출력되는 빈 껍데기 결과 반환.
* **원인**:
  - 브라우저 단에서 Base64로 인코딩된 이미지 페이로드가 정밀한 OCR 프롬프트 지시 없이 단순 텍스트 프롬프트와 결합되어 처리됨.
  - 시스템 프롬프트가 이미지 속 복잡한 표/문서 레이아웃을 파싱하도록 강제하는 역할 지침(Role Instruction)이 부족했음.

---

## 3. 요구되는 역할, 기능 및 기술 스택 (Role, Feature, Skills)

### 1) 필요한 역할 (Roles)
* **AI 프롬프트 엔지니어 / 시스템 아키텍트**:
  - 멀티모달 입력(스크린샷 이미지)에서 핵심 텍스트를 누락 없이 읽고, 표준 69개 직무 데이터셋과 정확히 대조하여 JSON 형태로 출력하도록 스키마 및 프롬프트 최적화.
* **프론트엔드 / 인터페이스 개발자**:
  - 클립보드 이미지 처리, 썸네일 관리, 비동기 상태 표시(로딩 스피너), 파싱된 JSON 데이터의 동적 렌더링 구현.
* **백엔드/API 연동 엔지니어 (선택적 권장)**:
  - 브라우저 직접 호출로 인한 CORS 및 모델 버전 변경 이슈를 방어하기 위한 가벼운 로컬 프록시(Python Flask/FastAPI 또는 Node.js) 구성.

### 2) 핵심 필요 기능 (Key Features)
| 구분 | 기능 명세 | 세부 요구사항 |
| :--- | :--- | :--- |
| **입력 모듈** | 클립보드 & 멀티모달 입력 | `Ctrl + V` 이미지 캡처 처리, 이미지 썸네일 미리보기/삭제, 텍스트 동시 입력 |
| **기준표 모듈** | 엑셀 데이터 탑재 | `교육행정 업무.xlsx`의 69개 표준 직군/직종/직렬/직무 데이터 내장 |
| **추론 모듈** | 지능형 분류 및 필터링 | - 표준 직무 일치 시: 상위 목표 도출 + 1~2문장 일지 작성<br>- 시트 미포함 시: '기타' 분류 및 목표 등록 제외 |
| **출력 모듈** | 구조화된 직무일지 렌더링 | 직무 분류 카드, 상위 목표, 업무내용/특이사항/기타업무 3분할 뷰 |

### 3) 필요 기술 및 스킬셋 (Tech Skills)
* **Languages & Frameworks**: HTML5, CSS3 (Modern Flex/Grid UI), Vanilla JavaScript (ES6+ Async/Await, Fetch API)
* **AI & LLM Integration**:
  - Google Gemini API (`@google/genai` SDK 또는 최신 REST API)
  - Multimodal Structured Output (JSON Schema enforcement)
  - Vision Context Extraction (OCR & Document Layout Parsing)
* **Tools & Environment**:
  - Visual Studio Code
  - Git / GitHub Version Control (`main` 브랜치)
  - Python / Pandas (사전 엑셀 데이터 가공 및 필요 시 로컬 백엔드 서버 구성)

---

## 4. 차기 해결 전략 (Next Action Plan)
1. **안정적인 모델 엔드포인트 고정**: 최신 API 스펙에 맞는 공식 SDK 사용 또는 확실하게 검증된 모델명(`gemini-2.0-flash`)으로 통일.
2. **프롬프트 튜닝 강화**: 이미지 속의 글자를 먼저 텍스트로 추출(Vision OCR)한 뒤 직무 목록과 유사도 대조를 진행하도록 2단계 추론 구조 설계.
3. **로컬 개발 서버 도입 검토**: `file://` 직접 실행 대신 간단한 Live Server나 경량 파이썬 웹서버(`python -m http.server`)를 통해 API 통신 안정성 확보.